/**
 * /organizer/register bu layout altında; oturum zorunluluğu (app) grubundadır.
 */
export default function OrganizerRootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
